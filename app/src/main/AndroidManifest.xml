<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".PlanifyApp"
        android:theme="@style/Theme.Planify">
        <activity
            android:name=".SesionesProximasActivity"
            android:exported="false" />
        <activity android:name=".DetalleSesionActivity" />
        <activity
            android:name=".AgregarSesionActivity"
            android:exported="false" />
        <activity
            android:name=".SesionesActivity"
            android:exported="false" />
        <activity
            android:name=".EvaluacionesActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Planify">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>